version: '3.9'

services:
  guardrail:
    build: .
    image: guardrail-backend:latest
    container_name: guardrail
    restart: always
    ports:
      - '8000:8000'
    env_file: .env
    network_mode: host
